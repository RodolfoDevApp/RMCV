export interface IconConfig {
  icon: string;      // nombre del icono Material
  size: number;      // tamaño en px
  color?: string;    // color CSS (p.ej. '#a4196c' o 'red')
  spin: boolean;     // gira o no
}
